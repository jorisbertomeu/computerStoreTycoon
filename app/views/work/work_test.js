'use strict';

describe('CST.work module', function() {

  beforeEach(module('CST.work'));

  describe('work controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var workCtrl = $controller('workCtrl');
      expect(workCtrl).toBeDefined();
    }));

  });
});
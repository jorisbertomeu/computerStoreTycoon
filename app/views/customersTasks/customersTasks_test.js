'use strict';

describe('CST.customersTasks module', function() {

  beforeEach(module('CST.customersTasks'));

  describe('customersTasks controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var customersTasksCtrl = $controller('customersTasksCtrl');
      expect(customersTasksCtrl).toBeDefined();
    }));

  });
});
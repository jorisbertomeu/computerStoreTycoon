'use strict';

describe('CST.myStore module', function() {

  beforeEach(module('CST.myStore'));

  describe('myStore controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var myStoreCtrl = $controller('myStoreCtrl');
      expect(myStoreCtrl).toBeDefined();
    }));

  });
});
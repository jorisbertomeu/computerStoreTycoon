'use strict';

describe('CST.management module', function() {

  beforeEach(module('CST.management'));

  describe('management controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var managementCtrl = $controller('managementCtrl');
      expect(managementCtrl).toBeDefined();
    }));

  });
});
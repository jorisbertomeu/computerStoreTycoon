'use strict';

describe('CST.stock module', function() {

  beforeEach(module('CST.stock'));

  describe('stock controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var stockCtrl = $controller('stockCtrl');
      expect(stockCtrl).toBeDefined();
    }));

  });
});